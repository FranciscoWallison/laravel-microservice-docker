import * as React from 'react';
import {Form} from "./Form";
import {Page} from "../../components/Page";

const PageForm = () => {
    return (
        <Page title={'Adicionar membro de elenco'}>
            <Form/>
        </Page>
    );
};

export default PageForm;